import json


def generate_code_prompt_harmony(blueprint) -> str:
    """
    根據高階功能藍圖,生成「全新建立」專案的提示 (Harmony 格式)。

    此函式採用 Harmony 格式，將角色、目標、藍圖、指令、規格和限制
    等元素清晰分離，引導 AI 更有條理地進行系統設計與全端開發。

    Args:
        blueprint (list or dict): 待開發應用的高階功能清單。

    Returns:
        str: 一個完整、格式化好的 Harmony Prompt 字串。
    """

    system_content = """我是一位專家級的 Python 全端工程師與系統設計師，專精於 Flask Web 應用程式開發。
    推理: 高
    
    # 有效頻道: analysis, final。每個訊息都必須包含頻道。
    
    你的任務是根據提供的功能藍圖，從零開始建構一個完整、現代化、可立即部署的 Flask Web 應用程式。
    
    你必須具備以下能力：
    - 深度理解業務需求並轉換為技術實現
    - 設計清晰的資料庫架構和 API 結構
    - 創建現代化、響應式的使用者介面
    - 實現健壯的錯誤處理和安全性機制
    - 確保程式碼的可維護性和擴展性"""

    # --- Developer Content: 提供詳細的指令、規則和技術規格 ---
    # 預先處理錯誤修正的條件式文本，讓 f-string 更簡潔
    has_error_code = any(item.get("name") == "error_code" for item in blueprint)
    error_fix_instruction_task = (
        f"**錯誤修正：**\n藍圖中包含 error_code 欄位，請特別注意其中的問題描述，確保生成的程式碼能夠避免或解決這些問題。\n"
        if has_error_code
        else ""
    )
    error_fix_instruction_robustness = (
        f"\n3. **錯誤修正：** 特別注意藍圖中 error_code 欄位描述的問題，在相關功能中加入針對性的錯誤處理機制"
        if has_error_code
        else ""
    )

    developer_content = f"""# 核心指令

        ## 任務目標
        根據提供的程式功能開發藍圖，獨立設計並從零開始打造一個健壯、可立即部署的 Python Flask Web 服務。
        {error_fix_instruction_task}
        
        ## 絕對強制要求 (違反任一項即為失敗)
        1. **100% 功能實現** - 藍圖中的**每一個功能**都必須完整實現，遺漏任何一個都是失敗
        2. **100% Input 對應** - 藍圖中每個功能的 `input` 欄位都必須有對應的表單元素，遺漏任何一個都是失敗
        3. **使用繁體中文** - 所有前端顯示內容都必須使用繁體中文
        4. **只輸出程式碼** - 禁止添加任何解釋、介紹、摘要或對話
        5. **可立即部署** - 產生的程式碼必須可以直接執行
        6. **現代化設計** - 必須使用現代化的 UI/UX 設計，禁止簡陋的介面
        7. **欄位完整性** - 必須解析並實現藍圖中所有 input 欄位，包括資料庫欄位名稱
        8. **資料庫欄位與前端頁面對應** - 每個資料庫欄位都必須在前端頁面中有對應的輸入欄位
        9. **不需要 Cross Site Request Forgery (CSRF) 防護**

        ## 開發流程 (嚴格按順序執行)
        1. **深度需求分析** - 逐一分析藍圖中每個功能的業務邏輯、資料流和使用者體驗
        2. **Input 欄位解析** - 仔細解析每個功能的 input 欄位，識別所有必要的表單元素和資料庫欄位
        3. **資料庫架構設計** - 根據 `input` 和 `output` 描述設計完整的 SQLite 資料庫綱要
        4. **API 路由設計** - 為每個功能設計 RESTful 風格的 Flask 路由
        5. **前端介面實現** - 創建現代化、響應式的 HTML 模板
        6. **錯誤處理** - 實作完整的錯誤處理機制{error_fix_instruction_robustness}

        ## 技術規格要求
        ### 專案結構
        - `app.py` - 主應用程式檔案，包含所有路由和業務邏輯
        - `database.py` - 資料庫初始化和管理
        - `templates/` - HTML 模板檔案
        - `static/` - 靜態資源 (CSS, JS, 圖片)
        - `requirements.txt` - 依賴套件清單

        ### 後端技術要求
        - **框架**: Python Flask 2.x+
        - **資料庫**: SQLite 3
        - **錯誤處理**: 完整的 try-except 機制
        - **入口點**: 必須包含 `if __name__ == '__main__':` 啟動區塊

        ### 前端技術要求
        - **框架**: Tailwind CSS (CDN 版本)
        - **字體**: Google Fonts 'Inter'
        - **桌面優先**: 專為電腦端設計，確保在桌面瀏覽器上有最佳體驗
        - **互動性**: 適當的 JavaScript 增強使用者體驗

        ## UI/UX 設計準則 (必須嚴格遵守)
        ### 設計系統
        - **顏色系統**: 
          - Primary: #2563EB (藍色)
          - Secondary: #475569 (灰色)
          - Success: #10B981 (綠色)
          - Warning: #F59E0B (橙色)
          - Error: #EF4444 (紅色)
          - Background: #F8FAFC (淺灰)
        - **字體系統**:
          - H1: text-4xl font-bold (2.25rem)
          - H2: text-3xl font-bold (1.875rem)
          - H3: text-2xl font-semibold (1.5rem)
          - Body: text-base (1rem)
          - Small: text-sm (0.875rem)
        - **間距系統**: 使用 4 的倍數 (4, 8, 12, 16, 20, 24, 32, 40, 48, 64)
        - **圓角系統**: 統一使用 rounded-lg (6px)
        - **陰影系統**: 使用 shadow-sm, shadow-md, shadow-lg

        ### 元件設計規範
        - **按鈕**: 
          - 主要按鈕: bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700
          - 次要按鈕: border border-gray-300 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-50
          - 危險按鈕: bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700
        - **表單元素**:
          - 輸入框: border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500
          - 標籤: block text-sm font-medium text-gray-700 mb-1
        - **卡片**: bg-white rounded-lg shadow-md p-6
        - **表格**: min-w-full bg-white shadow-sm rounded-lg overflow-hidden

        ### 佈局規範
        - **容器**: max-w-7xl mx-auto px-4
        - **網格**: 使用 grid 和 flexbox 進行桌面佈局
        - **間距**: 使用 p-4, p-6, p-8 等標準間距
        - **桌面優化**: 專為桌面瀏覽器設計，使用固定寬度和標準佈局

        ## 程式碼品質要求
        ### 程式碼結構
        - **模組化**: 將功能分離到不同的函數中
        - **命名規範**: 使用清晰、描述性的變數和函數名稱
        - **註解**: 為複雜邏輯添加簡潔的註解
        - **錯誤處理**: 每個可能失敗的操作都要有適當的錯誤處理

        ## 輸出格式要求
        - **檔案標示**: 每個檔案都必須使用 `=== file: path/to/filename.ext ===` 格式
        - **純程式碼**: 絕對不能使用 ```python```、```html``` 等 markdown 標記
        - **完整性**: 每個檔案都必須是完整、可執行的程式碼
        - **依賴清單**: 必須包含 requirements.txt 檔案

        ## 品質檢查清單
        在生成程式碼前，請確保：
        1. 每個藍圖功能的 input 欄位都有對應的表單元素
        2. 所有表單都有適當的驗證和錯誤處理
        3. 資料庫設計符合正規化原則
        4. 前端介面現代化且使用者友善
        5. 程式碼結構清晰且易於維護
        6. 桌面端優化設計，確保在電腦瀏覽器上有最佳體驗
        7. **Input 欄位完整性** - 確保沒有遺漏任何 input 中提到的欄位
        8. **表單標籤中文化** - 所有表單標籤都使用繁體中文，避免英文欄位名"""

    # --- User Content: 放入本次任務的具體資料 ---
    user_content = f"""# 專案藍圖

        **藍圖功能總數：{len(blueprint)} 個功能**
        **要求：實現率必須達到 100%**

        ```json
        {json.dumps(blueprint, ensure_ascii=False, indent=2)}
        ```

        ## 特別注意事項
        1. **逐一檢查 input 欄位**: 每個功能的 input 欄位都必須有對應的表單元素
        2. **現代化設計**: 必須使用現代化的 UI/UX 設計，禁止簡陋的介面
        3. **完整功能實現**: 不能遺漏任何藍圖中描述的功能
        4. **繁體中文介面**: 所有使用者介面文字都必須使用繁體中文
        5. **桌面端設計**: 專為電腦瀏覽器設計，確保在桌面環境下有最佳使用體驗
        6. **Input 欄位解析**: 仔細解析每個功能的 input 字串，識別所有必要的表單欄位
        7. **表單標籤中文化**: 將英文資料庫欄位名轉換為中文表單標籤
        8. **欄位類型選擇**: 根據欄位用途選擇適當的 HTML 輸入類型
        9. **複雜表單處理**: 對於包含大量欄位的功能（如傳染病通報），使用分組或分頁設計
        10.**不需要 Cross Site Request Forgery (CSRF) 防護**"""
    return f"""<|start|>system<|message|>{system_content}<|end|>\n<|start|>developer<|message|>{developer_content}<|end|>\n<|start|>user<|message|>{user_content}<|end|>"""


def reduce_chunk_prompt_harmony(partial_codes: list, blueprint: list) -> str:
    """
    為多個程式碼區塊生成「合併與整合」任務的提示 (Harmony 格式)。

    此函式採用 Harmony 格式，為 AI 提供清晰的整合工作流程、合併規則、
    上下文參考及輸出限制，引導其將零散的程式碼片段整合成一個單一、
    完整、可執行的 Flask 專案。

    Args:
        partial_codes (str): 包含多個已轉換程式碼區塊的字串。
        blueprint (list): 高階功能需求清單，作為整合過程的最終指導方針。

    Returns:
        str: 一個完整、格式化好的 Harmony Prompt 字串。
    """

    # 檢查藍圖中是否存在 'error_code' 項目
    has_error_code = any(item.get("name") == "error_code" for item in blueprint)

    system_content = """你是軟體整合工程師，專精於 Python Flask 框架整合。
    推理: 高
    
    # 有效頻道: analysis, final。每個訊息都必須包含頻道。
    
    你的任務是將多個程式碼區塊合併成一個完整、可執行的 Flask 專案。
    
    你必須具備以下能力：
    - 深度理解程式碼結構和業務邏輯
    - 識別並解決程式碼衝突和重複問題
    - 確保所有功能完整實現
    - 創建統一、可維護的專案結構"""

    developer_content = f"""# 核心指令

        ## 任務目標
        將提供的數個程式碼區塊合併並重構成一個單一、完整且可直接執行的 Flask 專案。
        {f'特別注意：藍圖中包含 error_code 項目，請在整合時確保相關的錯誤處理機制被正確實現。' if has_error_code else ''}
        
        ## 絕對強制要求 (違反任一項即為失敗)
        **程式碼區塊實現** - 必須完整實現功能清單中定義的所有 input 欄位
        **使用繁體中文** - 所有前端顯示內容都必須使用繁體中文
        **只輸出程式碼** - 禁止添加任何解釋、介紹、摘要或對話
        **可立即執行** - 產生的程式碼必須可以直接執行
        **現代化設計** - 必須使用現代化的 UI/UX 設計，禁止簡陋的介面
        **資料庫欄位與前端頁面對應** - 每個資料庫欄位都必須在前端頁面中有對應的輸入欄位
        **邏輯檢查** - 確保所有函式邏輯正確
        **不需要 Cross Site Request Forgery (CSRF) 防護**

        ## 整合工作流程 (嚴格按順序執行)
        1. **深度分析** - 檢閱所有的程式碼和功能清單，理解每個區塊的用途和邏輯，確保功能接有對應的程式碼，如有缺失需補足
        2. **程式碼合併** - 將所有程式碼片段整合到統一的專案結構中
        3. **衝突解決** - 解決命名衝突和邏輯重複問題
        4. **完整性驗證** - 逐一對照功能清單 的 input 欄位，確保每個欄位都有對應的表單元素
        5. **品質檢查** - 確保程式碼符合所有要求
        6. **最終產出** - 生成完整可執行的專案程式碼

        ## 核心整合準則
        ### 程式碼保留原則
        - **不可刪除**: 不可刪除或忽略任何程式碼區塊中提供的程式碼行
        - **謹慎合併**: 除非出現無法共存的直接衝突，才可進行謹慎的合併或重構
        - **保留邏輯**: 務必保留所有原始業務邏輯

        ### 專案結構要求
        - **單一入口**: 所有路由統一放在 app.py 中，不使用 Flask Blueprint
        - **必要文件**: 專案必須包含 app.py、database.py、requirements.txt
        - **資料庫規範**: init_db() 函數必須在 Python 程式碼中直接定義 CREATE TABLE
        - **根路由**: 必須包含 @app.route('/') 作為主頁

        ### 藍圖實現要求
        - **完整實現**: 必須完整實現功能清單中定義的所有 input 欄位
        - **表單標籤**: HTML 表單的標籤必須使用繁體中文，禁止使用資料庫欄位名
        - **逐一檢查**: 整合完成後必須逐一對照功能清單的 input 欄位

        ### 程式碼品質要求
        - **功能完整**: 確保所有功能均可正常運作
        - **錯誤處理**: 必須包含適當的錯誤處理機制
        - **程式碼檢查**: 確保程式碼符合 PEP 8 標準，並通過所有測試

        ## 輸出格式要求
        - **檔案標示**: 每個檔案的開頭必須使用 `=== file: path/to/filename.ext ===` 格式
        - **純程式碼**: 絕對不能使用 ```python``` 等任何 markdown 標記
        - **完整性**: 每個檔案都必須是完整、可執行的程式碼
        - **繁體中文**: 所有前端 UI 顯示文字使用繁體中文"""

    user_content = f"""# 功能清單

        **功能清單功能總數：{len(blueprint)} 個功能**
        **要求：實現率必須達到 100%**

        # 功能清單
        ```json
        {json.dumps(blueprint, ensure_ascii=False, indent=2)}
        ```

        # 程式碼區塊

        {partial_codes}

        ## 特別注意事項
        1. **逐一檢查 input 欄位**: 每個功能的 input 欄位都必須有對應的表單元素
        2. **現代化設計**: 必須使用現代化的 UI/UX 設計，禁止簡陋的介面
        3. **完整功能實現**: 不能遺漏任何功能清單中描述的功能
        4. **繁體中文介面**: 所有使用者介面文字都必須使用繁體中文
        5. **桌面端設計**: 專為電腦瀏覽器設計，確保在桌面環境下有最佳使用體驗
        6. **Input 欄位解析**: 仔細解析每個功能的 input 字串，識別所有必要的表單欄位
        7. **表單標籤中文化**: 將英文資料庫欄位名轉換為中文表單標籤
        8. **欄位類型選擇**: 根據欄位用途選擇適當的 HTML 輸入類型
        9. **複雜表單處理**: 對於包含大量欄位的功能（如傳染病通報），使用分組或分頁設計
        10. **不需要 Cross Site Request Forgery (CSRF) 防護**"""

    return f"""<|start|>system<|message|>{system_content}<|end|>\n<|start|>developer<|message|>{developer_content}<|end|>\n<|start|>user<|message|>{user_content}<|end|>"""


def generate_verification_prompt_harmony(
    blueprint, initial_solution: str, previous_release_note: str | None = None
) -> str:
    """
    生成驗證提示 (Verification Prompt) - 使用 Harmony 格式。

    此函式採用與 CT 相同的 Harmony 格式結構，但在 developer content 中加入驗證指令，
    要求 LLM 先驗證提供的解決方案，然後逐步找出正確的程式碼完成。

    Args:
        blueprint (list or dict): 待開發應用的高階功能清單。
        initial_solution (str): 提供的解決方案程式碼內容。
        previous_release_note (str | None): 之前生成的 release_note.md 內容（可選）。

    Returns:
        str: 一個完整、格式化好的 Harmony Prompt 字串。
    """

    system_content = """我是一位專家級的 Python 全端工程師與系統設計師，專精於 Flask Web 應用程式開發。
    推理: 高
    
    # 有效頻道: analysis, final。每個訊息都必須包含頻道。
    
    你的任務是根據提供的功能藍圖，從零開始建構一個完整、現代化、可立即部署的 Flask Web 應用程式。
    
    你必須具備以下能力：
    - 深度理解業務需求並轉換為技術實現
    - 設計清晰的資料庫架構和 API 結構
    - 創建現代化、響應式的使用者介面
    - 實現健壯的錯誤處理和安全性機制
    - 確保程式碼的可維護性和擴展性"""

    # --- Developer Content: 提供詳細的指令、規則和技術規格 ---

    developer_content = f"""# 核心指令

        ## 任務目標
        根據提供的程式功能開發藍圖，獨立設計並從零開始打造一個健壯、可立即部署的 Python Flask Web 服務。
        
        ## 驗證流程（重要）
        **已提供一個解決方案。你應先檢查該解決方案是否滿足問題的要求；若未滿足，請逐步思考以找到正確的程式碼完成。**
        
        {f'**重要：已提供之前檢查的結果（release_note.md）。請專注於修改其中提到的錯誤或漏掉的點。**' if previous_release_note else ''}
        
        1. **驗證解決方案** - 仔細檢查提供的解決方案，確認它是否滿足以下要求：
           - 是否完整實現了藍圖中的所有功能
           - 是否正確處理了所有 input 欄位
           - 是否解決了錯誤報告中提到的問題（如有）
           - 是否符合所有技術規格和設計要求
        
        2. **識別問題** - 如果解決方案存在問題，請明確指出：
           - 缺失的功能或欄位
           - 邏輯錯誤或不一致
           - 不符合規範的部分
        
        3. **逐步修正** - 如果解決方案不完整或不正確，請一步一步思考並生成正確的程式碼完成：
           - 先修正最關鍵的問題
           - 確保所有功能完整實現
           - 驗證修正後的程式碼符合所有要求
        
        ## 絕對強制要求 (違反任一項即為失敗)
        1. **100% 功能實現** - 藍圖中的**每一個功能**都必須完整實現，遺漏任何一個都是失敗
        2. **100% Input 對應** - 藍圖中每個功能的 `input` 欄位都必須有對應的表單元素，遺漏任何一個都是失敗
        3. **使用繁體中文** - 所有前端顯示內容都必須使用繁體中文
        4. **只輸出程式碼** - 禁止添加任何解釋、介紹、摘要或對話
        5. **可立即部署** - 產生的程式碼必須可以直接執行
        6. **現代化設計** - 必須使用現代化的 UI/UX 設計，禁止簡陋的介面
        7. **欄位完整性** - 必須解析並實現藍圖中所有 input 欄位，包括資料庫欄位名稱
        8. **資料庫欄位與前端頁面對應** - 每個資料庫欄位都必須在前端頁面中有對應的輸入欄位

        ## 開發流程 (嚴格按順序執行)
        1. **驗證解決方案** - 檢查提供的解決方案是否滿足所有要求
        2. **深度需求分析** - 逐一分析藍圖中每個功能的業務邏輯、資料流和使用者體驗
        3. **Input 欄位解析** - 仔細解析每個功能的 input 欄位，識別所有必要的表單元素和資料庫欄位
        4. **資料庫架構設計** - 根據 `input` 和 `output` 描述設計完整的 SQLite 資料庫綱要
        5. **API 路由設計** - 為每個功能設計 RESTful 風格的 Flask 路由
        6. **前端介面實現** - 創建現代化、響應式的 HTML 模板

        ## 技術規格要求
        ### 專案結構
        - `app.py` - 主應用程式檔案，包含所有路由和業務邏輯
        - `database.py` - 資料庫初始化和管理
        - `templates/` - HTML 模板檔案
        - `static/` - 靜態資源 (CSS, JS, 圖片)
        - `requirements.txt` - 依賴套件清單

        ### 後端技術要求
        - **框架**: Python Flask 2.x+
        - **資料庫**: SQLite 3
        - **錯誤處理**: 完整的 try-except 機制
        - **入口點**: 必須包含 `if __name__ == '__main__':` 啟動區塊

        ### 前端技術要求
        - **框架**: Tailwind CSS (CDN 版本)
        - **字體**: Google Fonts 'Inter'
        - **桌面優先**: 專為電腦端設計，確保在桌面瀏覽器上有最佳體驗
        - **互動性**: 適當的 JavaScript 增強使用者體驗

        ## UI/UX 設計準則 (必須嚴格遵守)
        ### 設計系統
        - **顏色系統**: 
          - Primary: #2563EB (藍色)
          - Secondary: #475569 (灰色)
          - Success: #10B981 (綠色)
          - Warning: #F59E0B (橙色)
          - Error: #EF4444 (紅色)
          - Background: #F8FAFC (淺灰)
        - **字體系統**:
          - H1: text-4xl font-bold (2.25rem)
          - H2: text-3xl font-bold (1.875rem)
          - H3: text-2xl font-semibold (1.5rem)
          - Body: text-base (1rem)
          - Small: text-sm (0.875rem)
        - **間距系統**: 使用 4 的倍數 (4, 8, 12, 16, 20, 24, 32, 40, 48, 64)
        - **圓角系統**: 統一使用 rounded-lg (6px)
        - **陰影系統**: 使用 shadow-sm, shadow-md, shadow-lg

        ### 元件設計規範
        - **按鈕**: 
          - 主要按鈕: bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700
          - 次要按鈕: border border-gray-300 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-50
          - 危險按鈕: bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700
        - **表單元素**:
          - 輸入框: border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500
          - 標籤: block text-sm font-medium text-gray-700 mb-1
        - **卡片**: bg-white rounded-lg shadow-md p-6
        - **表格**: min-w-full bg-white shadow-sm rounded-lg overflow-hidden

        ### 佈局規範
        - **容器**: max-w-7xl mx-auto px-4
        - **網格**: 使用 grid 和 flexbox 進行桌面佈局
        - **間距**: 使用 p-4, p-6, p-8 等標準間距
        - **桌面優化**: 專為桌面瀏覽器設計，使用固定寬度和標準佈局

        ## 程式碼品質要求
        ### 程式碼結構
        - **模組化**: 將功能分離到不同的函數中
        - **命名規範**: 使用清晰、描述性的變數和函數名稱
        - **註解**: 為複雜邏輯添加簡潔的註解
        - **錯誤處理**: 每個可能失敗的操作都要有適當的錯誤處理

        ## 輸出格式要求
        - **檔案標示**: 每個檔案都必須使用 `=== file: path/to/filename.ext ===` 格式
        - **純程式碼**: 絕對不能使用 ```python```、```html``` 等 markdown 標記
        - **完整性**: 每個檔案都必須是完整、可執行的程式碼
        - **依賴清單**: 必須包含 requirements.txt 檔案

        ## 品質檢查清單
        在生成程式碼前，請確保：
        1. 每個藍圖功能的 input 欄位都有對應的表單元素
        2. 所有表單都有適當的驗證和錯誤處理
        3. 資料庫設計符合正規化原則
        4. 前端介面現代化且使用者友善
        5. 程式碼結構清晰且易於維護
        6. 桌面端優化設計，確保在電腦瀏覽器上有最佳體驗
        7. **Input 欄位完整性** - 確保沒有遺漏任何 input 中提到的欄位
        8. **表單標籤中文化** - 所有表單標籤都使用繁體中文，避免英文欄位名"""

    # --- User Content: 放入本次任務的具體資料 ---
    user_content = f"""# 專案藍圖

        **藍圖功能總數：{len(blueprint)} 個功能**

        ```json
        {json.dumps(blueprint, ensure_ascii=False, indent=2)}
        ```

        # 初始解決方案

        以下是一個提供的解決方案，請先驗證它是否滿足所有要求。如果存在問題，請逐步思考並生成正確的程式碼完成。

        ```
        {initial_solution}
        ```"""

    # Add previous release note if exists
    if previous_release_note:
        user_content += f"""

        # 之前檢查的結果（藍圖對應區塊）

        以下是之前檢查程式碼後生成的 Release Note 中的「藍圖對應」區塊，其中明確標示了每個功能的實作狀態、錯誤、漏掉的點和未實現的項目。
        **請專注於修改這個區塊中提到的問題，特別是標示為「未實現」或「未達成」的項目。**

        ```
        {previous_release_note}
        ```

        **重要提醒**：
        - 請仔細閱讀上述「藍圖對應」區塊中提到的所有問題
        - 優先修正區塊中明確標示的錯誤或漏掉的點
        - 特別關注標示為「未實現」、「未達成」或「藍圖中有但程式碼中缺失」的項目
        - 確保修正後的程式碼能夠解決區塊中提到的主要問題"""

    user_content += """

        ## 特別注意事項
        1. **驗證優先** - 首先檢查提供的解決方案是否滿足所有要求
        2. **逐一檢查 input 欄位**: 每個功能的 input 欄位都必須有對應的表單元素
        3. **現代化設計**: 必須使用現代化的 UI/UX 設計，禁止簡陋的介面
        4. **完整功能實現**: 不能遺漏任何藍圖中描述的功能
        5. **繁體中文介面**: 所有使用者介面文字都必須使用繁體中文
        6. **桌面端設計**: 專為電腦瀏覽器設計，確保在桌面環境下有最佳使用體驗
        7. **Input 欄位解析**: 仔細解析每個功能的 input 字串，識別所有必要的表單欄位
        8. **表單標籤中文化**: 將英文資料庫欄位名轉換為中文表單標籤
        9. **欄位類型選擇**: 根據欄位用途選擇適當的 HTML 輸入類型
        10. **複雜表單處理**: 對於包含大量欄位的功能（如傳染病通報），使用分組或分頁設計"""

    return f"""<|start|>system<|message|>{system_content}<|end|>\n<|start|>developer<|message|>{developer_content}<|end|>\n<|start|>user<|message|>{user_content}<|end|>"""


def generate_release_note_prompt_harmony(blueprint, verified_code: str) -> str:
    """
    生成 Release Note 檢查與生成提示 (Harmony 格式)。

    此函式專門用於檢查已驗證的程式碼是否符合藍圖要求，並生成詳細的 Release Note。

    Args:
        blueprint (list or dict): 待開發應用的高階功能清單。
        verified_code (str): 已驗證的程式碼內容。

    Returns:
        str: 一個完整、格式化好的 Harmony Prompt 字串。
    """

    system_content = """我是一位專業的軟體品質保證工程師與技術文件撰寫者，專精於程式碼審查與文件撰寫。
    推理: 高
    
    # 有效頻道: analysis, final。每個訊息都必須包含頻道。
    
    你的任務是嚴格檢查已驗證的程式碼是否符合藍圖要求，並生成詳細的 Release Note。
    
    你必須具備以下能力：
    - 深度理解藍圖需求與程式碼實現之間的對應關係
    - 識別功能實現的完整性與正確性
    - 檢查 input 欄位與表單元素的對應關係
    - 評估資安風險並提供改善建議
    - 撰寫清晰、完整的技術文件
    - **解析多檔案程式碼結構**：理解以 `=== file: path ===` 分隔的多檔案格式"""

    developer_content = f"""# 核心指令

        ## 任務目標
        嚴格檢查已驗證的程式碼是否符合藍圖的所有要求，並生成一份詳細的 Release Note 文件。
        
        **重要原則：誠實報告，重視正確率**
        - 如果沒有達成的欄位，請誠實說明「未達成」，不要硬要說都已完成
        - 本報告重視的是**正確率**，沒有和藍圖 100% 重疊沒關係，但報告必須準確反映實際狀況
        - 必須明確標示：藍圖中有但程式碼中缺失的功能、input 欄位、資料庫欄位
        - 每個藍圖功能的 input 和 database 欄位都必須在報告中明確列出，無論是否已實現
        
        ## 程式碼結構說明（重要 - 必須先理解）
        提供的程式碼採用 **llm_dev.txt 多檔案合併格式**，結構如下：
        
        ### 檔案分隔格式
        - 每個檔案以 `=== file: path/to/filename.ext ===` 作為開頭標記
        - 標記後緊接著該檔案的完整內容
        - 下一個 `=== file:` 標記表示新檔案的開始
        
        ### 常見檔案類型與用途
        - `requirements.txt`：Python 依賴套件清單
        - `database.py`：資料庫初始化與模型定義，包含 `CREATE TABLE` 語句或 ORM 模型
        - `app.py`：Flask 主應用程式，包含所有 `@app.route()` 路由與業務邏輯
        - `templates/*.html`：Jinja2 HTML 模板，包含前端表單與 UI
        - `static/*`：靜態資源檔案（CSS, JS, 圖片）
        
        ### 多版本處理規則
        - 同一檔案可能在程式碼中出現多次（代表不同迭代版本）
        - **請以最後出現的版本為準**進行檢查
        - 若有版本衝突，以較完整的版本為主
        
        ### 關鍵檢查位置說明
        - **資料庫欄位**：查找 `database.py`，搜尋關鍵字 `CREATE TABLE`, `Column(`, `db.Column`
        - **API 路由**：查找 `app.py`，搜尋關鍵字 `@app.route(`
        - **前端表單欄位**：查找 `templates/*.html`，搜尋關鍵字 `<input`, `<select`, `<textarea`, `name=`
        - **表單提交目標**：查找 `templates/*.html`，搜尋關鍵字 `action=`, `method=`
        
        ## 檢查流程（必須嚴格執行）
        
        ### 步驟 0：解析程式碼結構（首要步驟）
        - 識別所有以 `=== file:` 開頭的檔案標記
        - 列出完整的檔案清單
        - 若同一檔案出現多次，記錄並以最後版本為準
        
        ### 步驟 1：功能完整性檢查 - 逐一對照藍圖中的每個功能：
           - 確認功能是否已實現
           - 檢查實現的完整性（部分完成/完全完成/未完成）
           - 識別缺失的功能或功能片段
           - 檢查對應的資料庫結構是否存在（例如 `database.py` / `models.py` 中的資料表與欄位定義）
        
        ### 步驟 2：Input 欄位對應檢查 - 對照每個功能的 input 欄位：
           - **必須列出藍圖中定義的所有 input 欄位**，無論是否已實現
           - 確認每個 input 欄位是否有對應的前端表單元素
           - 檢查表單元素的類型是否適當（text, number, date, select 等）
           - 驗證資料庫欄位與前端表單的對應關係（確認實際資料庫 schema 中存在對應欄位）
           - **明確標示缺失的 input 欄位**：如果藍圖中有但程式碼中沒有，必須清楚標示為「未實現」
        
        ### 步驟 3：前後端交叉一致性檢查（重要 - 必須執行）
        **此步驟要求前端 (HTML) 與後端 (Python) 相互檢查，確保完全一致。任何不一致都必須明確列出。**
        
        #### 3.1 前端 → 後端檢查（HTML 表單欄位是否有對應的後端處理）
        - 檢查每個 `templates/*.html` 中的表單欄位（`<input name="...">`, `<select name="...">`, `<textarea name="...">`）
        - 確認 `app.py` 中對應的路由是否有 `request.form.get('欄位名')` 或 `request.form['欄位名']` 來接收該欄位
        - 確認 `database.py` 中是否有對應的資料庫欄位來儲存該值
        - **列出「前端有但後端沒處理」的欄位清單**
        
        #### 3.2 後端 → 前端檢查（後端期望的欄位是否有對應的前端表單）
        - 檢查 `app.py` 中每個路由使用 `request.form.get()` 或 `request.form[]` 取得的欄位名
        - 確認對應的 HTML 模板中是否有該 `name` 屬性的表單元素
        - **列出「後端期望但前端沒提供」的欄位清單**
        
        #### 3.3 資料庫 → 前端/後端檢查（資料庫欄位是否有完整的前後端對應）
        - 檢查 `database.py` 中 `CREATE TABLE` 定義的每個欄位
        - 確認每個資料庫欄位是否：
          - 有對應的前端表單元素可輸入
          - 有對應的後端程式碼可接收並儲存
        - **列出「資料庫有但前端/後端未完整對應」的欄位清單**
        
        #### 3.4 一致性摘要統計（必須提供）
        由於欄位數量可能很多，請以**統計摘要**方式呈現，針對每個功能提供：
        
        **一致性統計格式：**
        - 藍圖定義欄位總數：X 個
        - 前端 HTML 已實現：Y 個（缺失：列出缺失欄位名稱）
        - 後端 Python 已處理：Z 個（缺失：列出缺失欄位名稱）
        - 資料庫 DB 已定義：W 個（缺失：列出缺失欄位名稱）
        - 完全一致欄位數：N 個
        - 不一致欄位數：M 個
        - **不一致欄位明細**：必須逐一列出不一致的欄位名稱及其缺失位置（前端/後端/資料庫）
        
        ### 步驟 4：程式碼品質檢查 - 評估程式碼的整體品質：
           - 檢查程式碼結構與組織
           - 驗證錯誤處理機制
           - 評估程式碼的可維護性
        
        ### 步驟 5：資安風險檢查 - 識別潛在的資安問題：
           - 輸入驗證機制
           - SQL 注入防護
           - XSS 防護
           - 檔案上傳安全性
           - 密鑰與憑證管理
           - 敏感資訊處理
           - 錯誤訊息處理
           - 會話與 Cookie 安全性
        
        ## Release Note 規範
        - **檔名**: `release_note.md`
        - **格式**: 純 Markdown 格式（不得使用 ``` 等程式碼圍欄）
        - **內容結構（請完整提供）**:
        
        1. **標題**: 例如「Release Notes — Verification 修正」
        
        2. **概覽**: 簡述此次驗證與修正的目標與成果
           - 整體實現率
           - 主要修正內容
           - 關鍵改進點
        
        3. **受影響檔案**: 列出本次輸出的檔案清單（如 app.py、requirements.txt、templates/... 等），與修改重點
           - 檔案名稱
           - 主要修改內容
           - 修改原因
        
        4. **檔案結構分析**: 列出程式碼中識別到的所有檔案
           - 檔案路徑（從 `=== file:` 標記中提取）
           - 檔案用途說明
           - 是否有重複版本（若有，說明採用哪個版本）
        
        5. **藍圖對應**: 逐項列出每個藍圖功能的實作狀態（已完成／未完成／部分完成），並清楚提供：
           - **功能名稱與描述**: 藍圖中定義的功能名稱與描述
           - **實作狀態**: 已完成 / 部分完成 / 未完成（請誠實標示，不要硬說已完成）
           - **藍圖中的 input 欄位清單**: 必須列出藍圖中定義的所有 input 欄位
           - **對應的前端表單欄位**: 列出實際程式碼中對應的 HTML 表單元素（input → UI 映射），如果沒有對應請標示「未實現」
           - **藍圖中的資料庫欄位清單**: 必須列出藍圖中定義的所有資料庫欄位
           - **對應的後端資料庫欄位**: 列出實際程式碼中對應的資料庫欄位（input → DB 映射），如果沒有對應請標示「未實現」
           - **前後端一致性檢查結果**: 必須提供一致性摘要表格，標示每個欄位在前端(HTML)、後端(Python)、資料庫(DB)的存在狀態
           - **不一致項目清單**: 明確列出所有前後端不一致的欄位，包括：
             - 前端有但後端沒處理的欄位
             - 後端期望但前端沒提供的欄位
             - 資料庫有但前端/後端未完整對應的欄位
           - **缺失項目明確標示**: 如果藍圖中有但程式碼中沒有，必須清楚標示為「藍圖中有但程式碼中缺失」
           - **未完成或部分完成的具體原因**: 詳細說明為何未完成或部分完成（誠實說明，不要硬湊理由）
           - **待補事項與明確修正計畫**: 包含優先順序與預期影響
        
        6. **資安建議**: 列出本次檢查發現的風險與具體改善措施：
           - **輸入驗證**: 所有表單與路由參數須強化驗證與型別檢查，避免使用 `eval/exec`
           - **SQL 注入**: 所有資料庫操作使用參數化查詢或 ORM，禁止字串拼接查詢
           - **XSS 防護**: 所有輸出內容須正確跳脫，模板避免直接渲染未處理的使用者輸入
           - **檔案上傳**: 限制副檔名與檔案大小，存放於安全路徑並驗證 MIME 類型
           - **密鑰管理**: 避免硬編碼金鑰與憑證，集中由環境變數或安全存儲載入
           - **敏感資訊**: 避免在日誌中輸出個資或憑證，必要時使用加密（配合現有 `utils.encryption`）
           - **錯誤回應**: 對外錯誤訊息需最小化資訊揭露，詳細堆疊只在伺服器端記錄
           - **會話與 Cookie**: 若使用登入機制，需設定 `HttpOnly`、`Secure` 並限制存活時間
        
        ## 輸出格式要求
        - **只輸出 Release Note**: 只輸出 release_note.md 的內容，不要輸出其他檔案
        - **純 Markdown**: 使用純 Markdown 格式，不得使用 ``` 等程式碼圍欄
        - **完整性**: 必須包含上述所有章節
        - **詳細性**: 每個功能都必須有詳細的檢查結果與對應關係說明
        - **準確性**: 檢查結果必須準確反映程式碼的實際狀態
        - **誠實性**: 如果沒有達成，請誠實標示「未達成」或「未實現」，不硬要說都已完成
        - **明確性**: 藍圖中有但程式碼中缺失的項目，必須明確標示，不可遺漏或隱瞞"""

    user_content = f"""# 專案藍圖

        **藍圖功能總數：{len(blueprint)} 個功能**

        ```json
        {json.dumps(blueprint, ensure_ascii=False, indent=2)}
        ```

        # 程式碼（llm_dev.txt 多檔案合併格式）

        以下是程式碼內容，採用 **多檔案合併格式**：
        - 每個檔案以 `=== file: path/to/filename.ext ===` 作為開頭標記
        - 同一檔案可能出現多次（代表不同迭代版本），請以**最後出現的版本**為準
        - 請先解析檔案結構，再進行功能檢查

        ```
        {verified_code}
        ```

        ## 檢查重點
        1. **解析檔案結構**: 首先識別所有 `=== file:` 標記的檔案，列出完整檔案清單
        2. **識別最終版本**: 若同一檔案出現多次，以最後版本為準
        3. **逐一對照**: 必須逐一對照藍圖中的每個功能，確認是否已實現
        4. **Input 欄位檢查**: 必須檢查每個功能的 input 欄位，列出藍圖中定義的所有 input，並標示哪些已實現、哪些未實現
        5. **資料庫欄位檢查**: 必須檢查每個功能的資料庫欄位，列出藍圖中定義的所有資料庫欄位，並標示哪些已實現、哪些未實現
        6. **前後端交叉一致性檢查（重要）**: 
           - 前端(HTML)每個表單欄位必須有對應的後端(Python)處理
           - 後端(Python)期望的每個欄位必須有對應的前端(HTML)表單
           - 資料庫(DB)每個欄位必須有完整的前端輸入與後端處理
           - **任何不一致都必須明確列出缺失的部分**
        7. **明確標示缺失**: 如果藍圖中有但程式碼中沒有，必須清楚標示為「藍圖中有但程式碼中缺失」
        8. **誠實報告**: 對於未完成或部分完成的功能，必須誠實詳細說明原因與待補事項，不要硬要說都已完成
        9. **資安檢查**: 必須進行完整的資安風險檢查並提供具體改善建議
        
        **再次強調**：本報告重視的是正確率，沒有和藍圖 100% 重疊沒關係，但報告必須準確、誠實地反映實際狀況。每個藍圖功能的 input 和 database 欄位都必須在報告中明確列出，無論是否已實現。"""

    return f"""<|start|>system<|message|>{system_content}<|end|>\n<|start|>developer<|message|>{developer_content}<|end|>\n<|start|>user<|message|>{user_content}<|end|>"""
